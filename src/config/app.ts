/** App identity. Change the name in one place. */
export const APP = {
  name: 'Vittara',
  tagline: 'India equity analytics',
  /** Shown in the header beside the wordmark while data is mocked. */
  dataStage: 'Mock data',
} as const
