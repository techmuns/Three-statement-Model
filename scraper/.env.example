# Screener.in credentials for the scraper.
#
# Copy this file to `scraper/.env` (which is gitignored) and fill in real
# values, OR export these variables into your environment. The scraper reads
# them at startup and never prints them or writes them to disk.

SCREENER_EMAIL=you@example.com
SCREENER_PASSWORD=your-screener-password
